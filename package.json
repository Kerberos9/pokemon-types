{
  "name": "pokemon-type-helper",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-scripts": "3.1.1",
    "react-select": "^3.0.8",
    "react-select-fast-filter-options": "^0.2.3",
    "serve": "^11"
  },
  "scripts": {
    "dev": "BROWSER=firefox-developer-edition && react-scripts start",
    "start": "https-serve -s build",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "node-fetch": "^2.6.0"
  }
}
